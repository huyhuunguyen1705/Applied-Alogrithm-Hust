/*
Cho 2 chuỗi s1, s2. HY vọng tìm chuỗi con chung dài nhất của 2 chuỗi trên.
Ta thu được một chuỗi con từ một chuỗi bằng cách xóa 0 hoặc nhiều phần tử mà không thay đổi vị trí tương đối của chuỗi đó.
Input: Dòng đầu tiên ghi s1, dòng thứ 2 ghi s2 (1 ≤ |s1|, |s2| ≤ 1000)
Output: In ra độ dài chuỗi con chung dài nhất của 2 chuỗi s1, s2

Ví dụ:
Input:
abc
acd
Output: 2. Giải thích: ac

Input:
abc
cba
Output: 1
*/

#include <bits/stdc++.h>

